<%= form_for([:admin, @alley]) do |f| %>
  <% if @alley.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@alley.errors.count, "error") %> prohibited this Bahn from being saved:</h2>

      <ul>
      <% @alley.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :active, 'Aktiv' %><br>
    <%= f.check_box :active %>
  </div>
  <div class="actions">
    <%= f.submit 'Speichern' %>
  </div>
<% end %>
